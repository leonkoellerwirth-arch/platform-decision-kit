/// <reference types="vite/client" />

/** Stamped by vite.config.ts at build time — see the `define` block there. */
declare const __APP_VERSION__: string;
/** Short commit the build came from; "" when git is unavailable, "abc1234+" when dirty. */
declare const __APP_COMMIT__: string;
